# pgvector / Postgres
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=bench
POSTGRES_PASSWORD=bench
POSTGRES_DB=bench

# Qdrant
QDRANT_HOST=localhost
QDRANT_PORT=6333

# Optional: OpenAI embeddings (if not using local model)
# OPENAI_API_KEY=sk-...
